# =================================
# 日本株ウォッチドッグ - 環境設定ファイル
# =================================

# 使用方法:
# 1. このファイルを .env にコピー: cp .env.example .env
# 2. 必要な値を実際の認証情報に置き換える
# 3. アプリを起動する

# ⚠️ セキュリティ重要事項:
# - .envファイルは絶対にGitにコミットしないでください
# - すべてのトークン・パスワードは定期的にローテーションしてください
# - 本番環境では最小権限の原則に従ってください

# =================================
# J Quants API 設定（株価データ取得）
# =================================

# Method 1: リフレッシュトークン（推奨・最も安全）
JQUANTS_REFRESH_TOKEN=your_refresh_token_here

# Method 2: メール/パスワード（非推奨・互換性のため残存）
# JQUANTS_EMAIL=your_email@example.com
# JQUANTS_PASSWORD=your_password

# =================================
# Gmail SMTP 設定（メール通知）
# =================================

# Gmail アカウント設定
GMAIL_USERNAME=your_email@gmail.com

# Gmail アプリパスワード（2段階認証必須）
# 取得方法: Googleアカウント > セキュリティ > アプリパスワード
GMAIL_APP_PASSWORD=your_16_digit_app_password

# Gmail 送信先メールアドレス（カンマ区切りで複数指定可能）
GMAIL_RECIPIENTS=your_email@example.com,another_email@example.com

# =================================
# Discord Webhook 設定（Discord通知）
# =================================

# Discord Webhook URL
# 取得方法: Discordサーバー > チャンネル設定 > 連携サービス > ウェブフック
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/your_webhook_id/your_webhook_token

# =================================
# LINE Notify 設定（サービス終了のため無効）
# =================================

# ⚠️ 注意: LINE Notify は2025年3月31日にサービス終了しました
# 現在この設定は使用されません
# LINE_NOTIFY_TOKEN=service_discontinued

# =================================
# その他の設定
# =================================

# デバッグモード（開発時のみ有効にする）
# DEBUG=true

# ログレベル（DEBUG, INFO, WARNING, ERROR）
# LOG_LEVEL=INFO

# API リクエスト間隔（秒）
# API_REQUEST_INTERVAL=2

# =================================
# 設定例とヘルプ
# =================================

# Gmail アプリパスワード取得手順:
# 1. Googleアカウントにログイン
# 2. セキュリティ > 2段階認証を有効化
# 3. セキュリティ > アプリパスワード > アプリを選択: メール
# 4. 生成された16桁のパスワードをGMAIL_APP_PASSWORDに設定

# Discord Webhook URL取得手順:
# 1. Discordサーバーで通知したいチャンネルを選択
# 2. チャンネル設定 > 連携サービス > ウェブフック > 新しいウェブフック
# 3. ウェブフック名を設定 > ウェブフックURLをコピー
# 4. コピーしたURLをDISCORD_WEBHOOK_URLに設定

# LINE Notify サービス終了のお知らせ:
# LINE Notify は2025年3月31日にサービス終了いたします
# 通知機能としてはDiscordまたはGmail通知をご利用ください